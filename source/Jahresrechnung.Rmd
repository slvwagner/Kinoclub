---
title: "Jahresabrechnung Kinoklub"
output: 
  html_document:
    css: Kinoklub_dark.css
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
act_date <- Sys.time()|>as.Date()
act_date <- paste0(lubridate::day(act_date),".", lubridate::month(act_date),".", lubridate::year(act_date))
```

Bericht wurde am `r act_date` erstellt.

## Filmvorführungen

### Eintritte

### Einnahmen Ticketverkauf
```{r}
df_temp <- df_Eintritt|>
  group_by(Platzkategorie, Verkaufspreis)|>
  reframe(Anzahl = sum(Anzahl),
          `Umsatz [CHF]` = sum(Umsatz, na.rm = T))|>
  arrange(desc(Verkaufspreis))

df_temp|>
  knitr::kable()
```


```{r}
df_temp <- df_temp|>
  reframe(`Summe [CHF]` = sum(`Umsatz [CHF]`, na.rm = T))|>
  arrange(desc(`Summe [CHF]`))


df_temp|>
  knitr::kable()
  
```


### Abgaben Ticketverkauf

```{r}
df_temp <- df_Abrechnung|>
  filter(is.na(`Verleiherrechnungsbetrag [CHF]`))|>
  select(Datum, Filmtitel, `Suisa Nummer`, Verleiher,`Verleiherabzug [CHF]`)|>
  arrange(desc(Datum))

if(nrow(df_temp)>0){
  c_text_output <- "#### Keine Verleiherabrechnung vorhanden\nFür die diese Film(e) gibt es keinen Verleiherabrechnung. Die Abgaben and den Verleiher können sich deshalb noch ändern:"
}
```

`r c_text_output`

```{r}
if(nrow(df_temp)>0){
  df_temp|>
  mutate(Datum = paste0(lubridate::day(Datum),
                        ".", lubridate::month(Datum),
                        ".", lubridate::year(Datum)))|>
    knitr::kable()
}
```


#### Suisaabgaben

```{r}
df_temp <- df_Abrechnung|>
  arrange(desc(Datum))|>
 mutate(Datum_display = paste0(lubridate::day(Datum),".", lubridate::month(Datum),".", lubridate::year(Datum)),
        Anfang = paste0(lubridate::hour(Anfang), ":", 
                         lubridate::minute(Anfang) |> as.character() |> formatC(width = 2, flag = "0")|>str_replace(SPC%R%"0", "00")
                        )
        )|>
  select(Datum, Datum_display, Filmtitel, `SUISA-Vorabzug [CHF]`)
```


<!-- ```{r} -->
<!-- DT::datatable(df_temp,  -->
<!--           options = list( -->
<!--             columnDefs = list( -->
<!--               # list(targets = 3,visible = F), -->
<!--               list(targets = 1, visible = FALSE),  # Hide the 'Datum' column -->
<!--               list(targets = 2, orderData = 0)    # Use the 'Datum' column for sorting 'Datum_display' -->
<!--             ) -->
<!--           ), -->
<!--           colnames = c(Datum = "Datum_display") -->
<!-- ) -->
<!-- ``` -->


```{r}
df_temp|>
  reframe(`Summe [CHF]` = sum(`SUISA-Vorabzug [CHF]`, na.rm = T))|>
  knitr::kable()
```

#### Verleiherabgaben / Rechnungen

```{r}
df_temp <- bind_rows(
  df_Abrechnung|>
    filter(is.na(`Verleiherrechnungsbetrag [CHF]`))|>
    rename(`Betrag [CHF]` = `Verleiherabzug [CHF]`)|>
    mutate(Rechnung = "keine",
           `Betrag [CHF]` = `Betrag [CHF]` + `MWST [CHF]`
           ),
  df_Abrechnung|>
    filter(!is.na(`Verleiherrechnungsbetrag [CHF]`))|>

    rename(`Betrag [CHF]` = `Verleiherrechnungsbetrag [CHF]`)|>
    mutate(Rechnung = "vorhanden"),
  )|>
  arrange(desc(Datum))|>
  mutate(Datum_display = paste0(lubridate::day(Datum),".", lubridate::month(Datum),".", lubridate::year(Datum)))

df_temp <- df_temp|>
  select(Datum, Datum_display, Filmtitel, `Suisa Nummer`, Verleiher, Rechnung,`Betrag [CHF]`)
  
```


```{r}
DT::datatable(df_temp,
          options = list(
            columnDefs = list(
              # list(targets = 3,visible = F),
              list(targets = 1, visible = FALSE),  # Hide the 'Datum' column
              list(targets = 2, orderData = 0)    # Use the 'Datum' column for sorting 'Datum_display'
            )
          ),
          colnames = c(Datum = "Datum_display")
)
```


```{r}
df_temp1 <- df_temp|>
  reframe(`Summe [CHF]` = sum(`Betrag [CHF]`)|>round5Rappen())

df_temp1|>
  knitr::kable()
```



### Differenz

```{r}
df_temp <- df_Abrechnung|>
  reframe(`Umsatz` = sum(`Umsatz [CHF]`, na.rm = T),
          `SUISA-Vorabzug`= -sum(`SUISA-Vorabzug [CHF]`, na.rm = T),
          `Verleiherabzug` = -df_temp1|>pull()
          )|>
  as.matrix()|>
  t()
colnames(df_temp) <- "Betrag [CHF]"

df_temp|>
  knitr::kable()
```



```{r}
Filmvorführungen <- df_temp|>
  as_tibble()|>
  reframe(`Summe [CHF]` = sum(`Betrag [CHF]`, na.rm = T))

Filmvorführungen|>
  knitr::kable(digits = 2)
```


## Event
### Eventeinnahmen
Einnahmen für den Event, z.B. Beiträgemitveranstalter, Eventsponsoring, ...
```{r}
df_temp <- Einnahmen_und_Ausgaben$Einnahmen|>
  filter(Kategorie == Einnahmen_und_Ausgaben$dropdown$`drop down`[1])|>
  arrange((Datum))|>
  left_join(df_show, by = "Datum")|>
  mutate(Datum = paste0(day(Datum),".",month(Datum),".",year(Datum)))|>
  select(Datum,Filmtitel,Bezeichnung, Betrag)|>
  rename(`Betrag [CHF]` = Betrag)
  

df_temp|>
  knitr::kable()
```

```{r}
df_temp <- df_temp|>
  reframe(`Summe [CHF]` = sum(`Betrag [CHF]`,na.rm = T))


df_temp|>
  knitr::kable(digits = 2)
```

### Eventausgaben
Alle Ausgaben für den Event, z.B. Werbung, Esswaren, Spesen, ...
```{r}
df_temp1 <-  Einnahmen_und_Ausgaben$Ausgaben|>
  filter(Kategorie == Einnahmen_und_Ausgaben$dropdown$`drop down`[1])|>
  arrange((Datum))|>
  left_join(df_show, by = c("Spieldatum" = "Datum"))|>
  mutate(Datum = paste0(day(Datum),".",month(Datum),".",year(Datum)))|>
  select(Datum,Filmtitel,Bezeichnung, Betrag)|>
  rename(`Betrag [CHF]` = Betrag)

df_temp1|>
  knitr::kable()
```


```{r}
df_temp1 <- df_temp1|>
  reframe(`Summe [CHF]` = sum(`Betrag [CHF]`,na.rm = T))


df_temp1|>
  knitr::kable(digits = 2)
```

### Differenz 
```{r}

tibble(Bezeichnung = c("Eventeinnahmen", "Eventausgaben"),
       `Betrag [CHF]` = c(df_temp|>pull(),
                          -df_temp1|>pull()
                          )
       )|>
  knitr::kable()
Event <- df_temp - df_temp1

Event|>
  knitr::kable()
```


## Kiosk
### Einahmen

```{r}
KioskEinnahmen <- df_Kiosk|>
  group_by(Verkaufsartikel, Verkaufspreis)|>
  reframe(Anzahl = sum(Anzahl),
          `Betrag [CHF]` = sum(Kassiert, na.rm = T))|>
  arrange(desc(`Betrag [CHF]`))

KioskEinnahmen|>
  knitr::kable()
```

```{r}
KioskEinnahmen <- KioskEinnahmen|>
  reframe(`Summe [CHF]` = sum(`Betrag [CHF]`))|>
  arrange(desc(`Summe [CHF]`))

KioskEinnahmen|>
  knitr::kable(digits = 2)
```


### Ausgaben
#### Einkauf Getränke
Die Getränke werden gemeinsam mit der Theaterbar eingekauft und der Lieferant ist Schüwo. 
```{r}
EinkaufGetränke <- df_Kiosk|>
  filter(Lieferant == "Schüwo")|>
  group_by(Verkaufsartikel,Verkaufspreis, Einkaufspreis)|>
  reframe(Anzahl = sum(Anzahl),
          `Betrag [CHF]` = sum(Einkaufspreis))|>
  arrange(desc(Anzahl))

EinkaufGetränke|>
  knitr::kable()
```

```{r}
EinkaufGetränke <- EinkaufGetränke|>
  reframe(`Summe [CHF]` = sum(`Betrag [CHF]`, na.rm = T))

EinkaufGetränke|>
  knitr::kable()
```


#### Einkauf Kino
```{r echo=FALSE}
EinkaufKino <- Einnahmen_und_Ausgaben$Ausgaben|>
  filter(Kategorie == Einnahmen_und_Ausgaben$dropdown$`drop down`[2])|>
    arrange(Datum)|>   mutate(Datum = paste0(day(Datum),".",month(Datum),".",year(Datum)))|>
  select(Datum, Bezeichnung, Betrag)|>
  rename(`Betrag [CHF]` = Betrag)

EinkaufKino|>
  knitr::kable()
```

```{r}
EinkaufKino <- EinkaufKino|>
  reframe(`Summe [CHF]` = sum(`Betrag [CHF]`, na.rm = T))

EinkaufKino|>  
  knitr::kable()
```

### Kasse
**Achtung!** \
Die Kioskkasse wird auch für Barauszahlung von stornierten Tickets genutzt. 

```{r}
df_temp <- df_manko_uerberschuss|>
  left_join(df_show, by = "Datum"
            ) |>
  arrange(Datum)|>
  select(Datum, Filmtitel, `Überschuss / Manko`) |>
  rename(`Überschuss / Manko [CHF]` = `Überschuss / Manko`) |>
  arrange((Datum))|>
  mutate(Datum = paste0(day(Datum),".",month(Datum),".",year(Datum)))

df_temp|>
  knitr::kable()
```


```{r}
Kasse <- df_temp|>
  select(-Datum)|>
  reframe(`Überschuss / Manko [CHF]` = sum(`Überschuss / Manko [CHF]`, na.rm = T))

Kasse|>
  knitr::kable(digits = 2)
```


### Differenz
```{r}
Kiosk <- tibble(Bezeichnung = c("Einnahmen","Einkauf Getränke", "Einkauf Kino", "Kasse: Manko/Überschuss"),
                `Betrag [CHF]` = c(KioskEinnahmen|>pull(),
                                   -(EinkaufGetränke|>pull()),
                                   -(EinkaufKino|>pull()),
                                   Kasse|>pull()
                                   )
                )

Kiosk|>
  knitr::kable()

```

```{r}
Kiosk <- Kiosk|>
  reframe(`Summe [CHF]` = sum(`Betrag [CHF]`,na.rm = T))

Kiosk|>
  knitr::kable()
```


## Abos / Kinogutscheine
Kurzfristiges zinsloses Fremd-Kapital.  

### Einnahmen
#### Abos
```{r}
all_abos|>
  filter(between(creation, 
                 as.Date(paste0(Abrechungsjahr,"-01-01")), 
                 as.Date(paste0(Abrechungsjahr,"-12-31"))
                 ),
         Abo == "gutschein"
         )

df_Abo_Verkauf|>
  mutate(Datum = paste0(day(Datum),".", month(Datum), ".", year(Datum)))|>
  knitr::kable()
```


```{r}
df_temp <- df_Abo_Verkauf|>
  reframe(`Summe Anzahl` = sum(Anzahl),
          `Summe Betrag [CHF]` = sum(Betrag)
         )

df_temp|>
  knitr::kable()
```

#### Kinogutscheine
```{r}
df_Kinogutschein_Verkauf|>
  mutate(Datum = paste0(day(Datum),".", month(Datum), ".", year(Datum)))|>
  knitr::kable()
```

```{r}
df_temp1 <- df_Kinogutschein_Verkauf|>
  reframe(`Summe Anzahl` = sum(Anzahl),
          `Summe Betrag [CHF]` = sum(Betrag)
         )

df_temp1|>
  knitr::kable()
```

#### Summe
```{r}
df_temp <- bind_rows(df_temp, df_temp1)|>
  mutate(Bezeichnung = c("Abos", "Kinogutscheine"))

df_temp|>
  select(Bezeichnung, `Summe Anzahl`, `Summe Betrag [CHF]` )|>
  knitr::kable()

```

```{r}
AboGutscheinEinahmen <- df_temp|>
  reframe(`Summe [CHF]` = sum(`Summe Betrag [CHF]`, na.rm = T))

AboGutscheinEinahmen|>
  knitr::kable()
```

### Eingelöst
#### Abos
```{r}
df_temp1 <- df_Eintritt |>
  mutate(Datum = paste0(day(Datum),".", month(Datum), ".", year(Datum)))|>
  filter(str_detect(Platzkategorie, "Abo")) |>
    group_by(Datum, Platzkategorie, Verkaufspreis)|>
  reframe(Anzahl = sum(Anzahl),
          `Umsatz [CHF]` = sum(Umsatz))

df_temp1|>
  knitr::kable()
```


```{r}
df_temp1 <- df_temp1|>
  reframe(`Summe [CHF]` = sum(`Umsatz [CHF]`, na.rm = T))

df_temp1|>
  knitr::kable()
```

#### Kinogutscheine 
```{r}
df_temp2 <- df_Kinogutschein_Eingeloest|>
  mutate(Datum = paste0(day(Datum),".", month(Datum), ".", year(Datum)),
         Einzelpreis = abs(Betrag)/Anzahl,
         Betrag = abs(Betrag))|>
  select(Datum, Verkaufsartikel, Einzelpreis, Anzahl, Betrag)|>
  rename(`Betrag [CHF]` = Betrag)

df_temp2|>
  knitr::kable()
```

```{r}
df_temp2 <- df_temp2|>
  reframe(`Summe [CHF]` = sum( abs(`Betrag [CHF]`), na.rm = F))

df_temp2|>
  knitr::kable()
```

#### Summe
```{r}
df_temp3 <- tibble(`Summe [CHF]` = df_temp1$`Summe [CHF]` + df_temp2$`Summe [CHF]`)

df_temp3|>
  knitr::kable()
```


### Kurzfristiges zinsloses Fremdkapital
```{r}
df_temp <- bind_cols(tibble(Bezeichnung = c("Verkaufte Abos", "Verkaufte Kinogutscheine","Eingelöste Abos", "Eingelöste Kinogutscheine")),
          bind_rows(df_Abo_Verkauf|>
                      reframe(`Summe [CHF]` = sum(Betrag,na.rm = T)),
                    df_Kinogutschein_Verkauf|>
                      reframe(`Summe [CHF]` = sum(Betrag,na.rm = T)),
                    -df_temp1, 
                    -df_temp2)
          )|>
  rename(`Betrag [CHF]` = `Summe [CHF]`)

df_temp|>
  knitr::kable()
```

```{r}
`Kurzfristiges zinsloses Fremdkapital` <- df_temp|>
  reframe(`Summe [CHF]` = sum(`Betrag [CHF]`, na.rm = T))
`Kurzfristiges zinsloses Fremdkapital`|>
  knitr::kable()
```


## Werbung
### Einnahmen
```{r echo=FALSE}
df_temp <- Einnahmen_und_Ausgaben$Einnahmen|>
  filter(Kategorie == Einnahmen_und_Ausgaben$dropdown$`drop down`[7])|>
  select(Datum, Firmennamen , Betrag)|>
  rename(`Betrag [CHF]` = Betrag)|>
    arrange(Datum)|>   
  mutate(Datum = paste0(day(Datum),".",month(Datum),".",year(Datum)))

df_temp|>
  knitr::kable()
```

```{r echo=FALSE}
df_temp <- Einnahmen_und_Ausgaben$Einnahmen|>
  filter(Kategorie == Einnahmen_und_Ausgaben$dropdown$`drop down`[7])|>
  reframe(`Summe [CHF]` = sum(Betrag))

df_temp|>
  knitr::kable()
```

### Ausgaben
```{r echo=FALSE}
df_temp1 <- Einnahmen_und_Ausgaben$Ausgaben|>
  filter(Kategorie == Einnahmen_und_Ausgaben$dropdown$`drop down`[7])|>
  select(Datum, Bezeichnung , Firmennamen, Betrag)|>
    arrange(Datum)|>   
  mutate(Datum = paste0(day(Datum),".",month(Datum),".",year(Datum)))|>
  rename(`Betrag [CHF]` = Betrag)

df_temp1|>
  knitr::kable()

```

```{r echo=FALSE}
df_temp1 <- Einnahmen_und_Ausgaben$Ausgaben|>
  filter(Kategorie == Einnahmen_und_Ausgaben$dropdown$`drop down`[7])|>
  reframe(`Summe [CHF]` = sum(Betrag))

df_temp1|>
  knitr::kable()

```

### Differenz
```{r}
df_temp <- tibble(Bezeichnung = c("Einnahmen", "Ausgaben"),
       `Betrag [CHF]` = c(df_temp|>pull(),
                          -df_temp1|>pull()
                          )
       )
df_temp|>
  knitr::kable()

Werbung <- (Einnahmen_und_Ausgaben$Einnahmen|>
  filter(Kategorie == Einnahmen_und_Ausgaben$dropdown$`drop down`[7])|>
  reframe(`Betrag [CHF]` = sum(Betrag, na.rm = T)) - 
  Einnahmen_und_Ausgaben$Ausgaben|>
  filter(Kategorie == Einnahmen_und_Ausgaben$dropdown$`drop down`[7])|>
  reframe(`Betrag [CHF]` = sum(Betrag, na.rm = T)))

Werbung|>
  knitr::kable()
```



## Vermietungen 

### Einnahmen
```{r echo=FALSE}
df_temp <- Einnahmen_und_Ausgaben$Einnahmen|>
  filter(Kategorie == "Vermietung")|>
  select(Datum, Bezeichnung, Betrag)|>
    arrange(Datum)|>   
  mutate(Datum = paste0(day(Datum),".",month(Datum),".",year(Datum)))|>
  rename(`Betrag [CHF]` = Betrag)

df_temp|>
  knitr::kable()

```

```{r echo=FALSE}
df_temp <- df_temp|>
  reframe(`Summe [CHF]`=sum(`Betrag [CHF]`, na.rm = T))
df_temp|>
  knitr::kable()
```

### Ausgaben
```{r echo=FALSE}
df_temp1 <- Einnahmen_und_Ausgaben$Ausgaben|>
  filter(Kategorie == Einnahmen_und_Ausgaben$dropdown$`drop down`[6])|>
  select(Datum, Bezeichnung, Betrag)|>
  rename(`Betrag [CHF]` = Betrag)|>
  arrange(Datum)|>   
  mutate(Datum = paste0(day(Datum),".",month(Datum),".",year(Datum)))

df_temp1|>
  knitr::kable()

```

```{r echo=FALSE}
df_temp1 <- df_temp1|>
  reframe(`Summe [CHF]`=sum(`Betrag [CHF]`, na.rm = T))

df_temp1|>
  knitr::kable()
```

### Differenz
```{r}
Vermietung <- tibble(Bezeichnung = c("Einnahmen", "Ausgaben"),
       `Summe [CHF]` = c(df_temp|>pull(),
                          -df_temp1|>pull()
                          )
       )

Vermietung|>
  knitr::kable()
```


```{r}
Vermietung <- (Einnahmen_und_Ausgaben$Einnahmen|>
  filter(Kategorie == Einnahmen_und_Ausgaben$dropdown$`drop down`[6])|>
  reframe(`Betrag [CHF]`=sum(Betrag, na.rm = T))-
  Einnahmen_und_Ausgaben[["Ausgaben"]]|>
  filter(Kategorie == "Vermietung")|>
  reframe(`Betrag [CHF]`=sum(Betrag, na.rm = T)))

Vermietung|>
  knitr::kable()
  
```

## Personalaufwand
```{r echo=FALSE}
df_temp <- Einnahmen_und_Ausgaben$Ausgaben|>
  filter(Kategorie == Einnahmen_und_Ausgaben$dropdown$`drop down`[3])|>
  select(Datum, Bezeichnung,Betrag)|>
  rename(`Betrag [CHF]` = Betrag)|>
  arrange(Datum)|>
  mutate(Datum = paste0(day(Datum),".",month(Datum),".",year(Datum)))

df_temp|>
  knitr::kable()

```

```{r echo=FALSE}
Personalaufwand <- df_temp|>
  reframe(`Summe [CHF]` = sum(`Betrag [CHF]`, na.rm = T))

Personalaufwand|>
  knitr::kable()

```

## Sonstiges

### Einnahmen
```{r echo=FALSE}
df_temp <- Einnahmen_und_Ausgaben$Einnahmen|>
  filter(Kategorie == Einnahmen_und_Ausgaben$dropdown$`drop down`[4])|>
  select(Datum, Bezeichnung,Betrag)|>
  rename(`Betrag [CHF]` = Betrag)|>
  arrange(Datum)|>   
  mutate(Datum = paste0(day(Datum),".",month(Datum),".",year(Datum)))

df_temp|>
  knitr::kable()

```

```{r echo=FALSE}
df_temp <- df_temp|>
  reframe(`Summe [CHF]` = sum(`Betrag [CHF]`, na.rm = T))

df_temp|>
  knitr::kable()

```

### Ausgaben
```{r echo=FALSE}
df_temp1 <- Einnahmen_und_Ausgaben$Ausgaben|>
  filter(Kategorie == Einnahmen_und_Ausgaben$dropdown$`drop down`[4])|>
  select(Datum, Bezeichnung,Betrag)|>
  rename(`Betrag [CHF]` = Betrag)|>
  arrange(Datum)|>
  arrange(Datum)|>   
  mutate(Datum = paste0(day(Datum),".",month(Datum),".",year(Datum)))

df_temp1|>
  knitr::kable()

```

```{r echo=FALSE}
df_temp1 <- df_temp1|>
  reframe(`Summe [CHF]` = sum(`Betrag [CHF]`))

df_temp1|>
  knitr::kable()

```

### Differenz
```{r}
df_temp <- tibble(Bezeichnung = c("Einnahmen", "Ausgaben"),
       `Betrag [CHF]` = c(df_temp|>pull(),
                          -df_temp1|>pull()
                          )
       )
df_temp|>
  knitr::kable()
```
```{r}
Sonstiges <- df_temp|>
  reframe(`Summe [CHF]` = sum(`Betrag [CHF]`))

Sonstiges|>
  knitr::kable()
```


## Gewinn / Verlust

```{r echo=FALSE}
if(nrow(df_keine_Rechnnung)>0) {
  writeLines("Achtung es sind noch nicht alle Filmabrechnungen vorhanden. Die Einnahmen aus dem Ticketverkauf werden sich noch ändern.")
}

```



```{r}
GewinnVerlust <- tibble(
  Kategorie = c("Filmvorführungen", "Event","Kiosk","Kurzfristiges zinsloses Fremdkapital","Werbung", "Vermietung", "Personalaufwand","Sonstiges"),
  `Betrag [CHF]` = c(
    Filmvorführungen |>pull(),
    Event|>pull(),
    Kiosk|>pull(),
    -`Kurzfristiges zinsloses Fremdkapital`|>pull(),
    Werbung|>pull(),
    Vermietung|>pull(),
    -1*(Personalaufwand|>pull()),
    Sonstiges|>pull()
  )
) 

GewinnVerlust|>
  knitr::kable()
```

```{r}
GewinnVerlust|>
  reframe("Gewinn / Verlust [CHF]" = sum(`Betrag [CHF]`, na.rm = T))|>
  knitr::kable()
```


\
\
\
\
\
\
